package stack;

public class Theory {
    
    //LIFO(last in first out data structure)

    //push pop peek all operations are done on the top element of the stack

    //using arrays > not preferred as arrays are immutable

    //if we want to make stack using lls then the head is the top that is we push from the last to
    //first so that the head becomes the top    
    
}
